name: nana_poc
description: Calm-tech personalized daily companion starter for NANA.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  cloud_functions: any
  firebase_messaging: any
  flutter_local_notifications: any
  geolocator: any
  geocoding: any
  shared_preferences: any
  google_fonts: any
  intl: any
  http: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
